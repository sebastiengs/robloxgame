-- Placeholder server script, just so the repo has something to sync.
-- Once you copy your real game code in, you can delete this file.
--
-- Anything ending in `.server.luau` in this folder becomes a Script
-- inside ServerScriptService when Rojo syncs it into Studio.

print("Server is running from GitHub!")
